<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FindMyBLEDevice.Views.AboutPage"
             xmlns:vm="clr-namespace:FindMyBLEDevice.ViewModels"
             Title="{Binding Title}"
             AutomationId="Page_About"
             >
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#96d1ff</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackLayout BackgroundColor="{StaticResource Accent}" VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                <ContentView Padding="0,40,0,40" VerticalOptions="FillAndExpand">
                    <Image Source="find_my_ble_device.png" VerticalOptions="Center" HeightRequest="64" AutomationId="AboutPage_Image"/>
                </ContentView>
            </StackLayout>
        </StackLayout>
        <ScrollView Grid.Row="1">
            <StackLayout Orientation="Vertical" Padding="30,24,30,24" Spacing="10">

                <Label Text="Welcome in Find My BLE Device" FontSize="Title" TextColor="Black" AutomationId="AboutPage_Title"/>
                <Label Text="The app is able to detect BLE devices and assign labels to it. In addition it helps tracking the device and navigating to the exact location of the device no matter if in short or long distance. This ensures an overview of all relevant devices at all times." TextColor="Black" FontSize="16" Padding="0,0,0,0" AutomationId="AboutPage_Description"/>
                
                <Button Margin="0,10,0,0" Text="Signal Strength Search" AutomationId="AboutPage_Btn_Signal"
                        Command="{Binding OpenStrengthPageCommand}"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White" 
                        />

                <Button Margin="0,10,0,0" Text="Geolocation Search" AutomationId="AboutPage_Btn_GPS"
                        Command="{Binding OpenMapPageCommand}"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White" />

            </StackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
