name: Trigger Both Builds

on:
  push:
    branches:
      - dev
  workflow_dispatch:

jobs:
  build-android: 
    uses: ./.github/workflows/android.yml
    
  build-ios: 
    uses: ./.github/workflows/iOS.yml
